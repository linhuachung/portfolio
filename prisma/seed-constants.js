/**
 * Constants for seed data
 *
 * NOTE: Only used for initial database setup (admin user creation).
 * User data is managed through the admin panel and stored in the database.
 */
export const SEED_CONSTANTS = {
  ADMIN: {
    USERNAME: 'admin',
    PASSWORD: 'admin',
    EMAIL: 'admin@example.com'
  }
};

